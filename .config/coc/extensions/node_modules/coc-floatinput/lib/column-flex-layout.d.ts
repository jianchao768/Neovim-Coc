import { Merge } from 'type-fest';
export interface BaseColumn {
    text: string;
    width: number;
}
export declare type ColumnLayout<C extends BaseColumn> = Merge<C, {
    line: number;
    colStart: number;
    colEnd: number;
}>;
export declare function columnsFlexLayout<C extends BaseColumn>(maxWidth: number, columns: C[]): ColumnLayout<C>[][];
