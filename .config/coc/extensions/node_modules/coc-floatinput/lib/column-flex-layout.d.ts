export interface BaseColumn {
    text: string;
    width: number;
}
export type ColumnLayout<C extends BaseColumn> = C & {
    line: number;
    colStart: number;
    colEnd: number;
};
export declare function columnsFlexLayout<C extends BaseColumn>(maxWidth: number, columns: C[]): ColumnLayout<C>[][];
