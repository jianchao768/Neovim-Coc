import { Input } from './Input';
export declare class NumberInput extends Input<number> {
    protected defaultString(): Promise<string>;
    protected valueToString(value: number): Promise<string>;
    protected stringToValue(str: string): Promise<number>;
    protected validateContent(str: string): Promise<boolean>;
}
