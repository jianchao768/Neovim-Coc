/// <reference types="node" />
import { FloatingWindow } from 'coc-helper';
import type { Disposable, ExtensionContext, WorkspaceConfiguration } from 'coc.nvim';
export declare class CocStatusManager implements Disposable {
    protected context: ExtensionContext;
    protected config: WorkspaceConfiguration;
    protected disposables: Disposable[];
    static create(context: ExtensionContext, config: WorkspaceConfiguration): Promise<CocStatusManager>;
    protected constructor(context: ExtensionContext, config: WorkspaceConfiguration, disposables?: Disposable[]);
    dispose(): void;
    protected _floatWin?: FloatingWindow;
    protected floatWin(): Promise<FloatingWindow>;
    protected autoCloseTimer?: NodeJS.Timeout;
    show(): Promise<void>;
    hide(): Promise<void>;
    enable(): Promise<void>;
    disable(): Promise<void>;
}
