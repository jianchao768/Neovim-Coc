'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const uuid_1 = require("uuid");
const logger_1 = require("../logger");
const manager_1 = tslib_1.__importDefault(require("./manager"));
const logger = (0, logger_1.createLogger)('linkedEditingManager');
class LinkedEditingRangeManager extends manager_1.default {
    register(selector, provider) {
        return this.addProvider({
            id: (0, uuid_1.v4)(),
            selector,
            provider
        });
    }
    /**
     * Multiple providers can be registered for a language. In that case providers are sorted
     * by their {@link workspace.match score} and the best-matching provider that has a result is used. Failure
     * of the selected provider will cause a failure of the whole operation.
     */
    async provideLinkedEditingRanges(document, position, token) {
        let items = this.getProviders(document);
        for (let item of items) {
            let res = await Promise.resolve(item.provider.provideLinkedEditingRanges(document, position, token));
            if (res != null)
                return res;
        }
        return null;
    }
}
exports.default = LinkedEditingRangeManager;
//# sourceMappingURL=linkedEditingRangeManager.js.map