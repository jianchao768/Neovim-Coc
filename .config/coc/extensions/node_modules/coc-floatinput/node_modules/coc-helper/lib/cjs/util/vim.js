"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.registerRuntimepath = void 0;
const coc_nvim_1 = require("coc.nvim");
async function registerRuntimepath(context) {
    const { nvim } = coc_nvim_1.workspace;
    const extensionPath = context.extensionPath;
    const paths = await nvim.runtimePaths;
    if (!paths.includes(extensionPath)) {
        await nvim.command(`execute 'noa set rtp+='.fnameescape('${extensionPath.replace(/'/g, "''")}')`);
    }
}
exports.registerRuntimepath = registerRuntimepath;
