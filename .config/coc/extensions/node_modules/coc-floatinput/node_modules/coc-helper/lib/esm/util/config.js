import { workspace } from 'coc.nvim';
import { generateUri } from './text';
export const getConfigLocal = (section) => (resource = generateUri(workspace.cwd)) => workspace.getConfiguration(section, resource);
